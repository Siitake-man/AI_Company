# Julesからしいたけさんへの引き継ぎ事項 (2026-07-13)

## 実施した内容

1. **`src/index.css` に DESIGN_SYSTEM の基盤を実装**
   - 依頼されたCSS変数、フォント、およびUIコンポーネントクラス (`.btn-primary`, `.panel-paper`, `.sidebar-wood`など) を `src/index.css` にすべて追加しました。

2. **`src/App.tsx` の Tailwind ハードコードを修正**
   - 既存の `bg-[#xxx]` や `border-[#xxx]` のようなハードコードされたカラーコードを、`index.css` に定義した `var(--color-*)` や新設したCSSクラス (`panel-paper`, `btn-primary` 等) を使うように置換しました。

3. **S2（プロジェクト一覧）画面の基本骨格実装**
   - `currentScreen` ステートに `"home"` を追加し、APIキーが登録されている場合は `"home"` を初期表示するように変更しました。
   - `useEffect` を追加して、SQLiteデータベースからプロジェクト一覧 (`projects`テーブル) と、選択中プロジェクトのメンバー情報 (`departments`テーブルをJOIN) を取得するロジックを実装しました。
   - 取得したデータを使用して、左サイドバーにプロジェクト一覧を表示し、右メインエリアにプロジェクトの詳細、統計情報、チームメンバーカードを表示する S2ダッシュボードの UI を実装しました。
   - メンバーの役割に応じた背景色 (`getRoleColor`) や、アバター用の絵文字プレースホルダー (`getEmojiForRole`) も実装しました。
   - 「会議を始める」ボタンも配置し、クリック時にはアラートが表示されるようにしています。

## 留意事項・理由

- **リファクタリング範囲**: 既存のロジックや state 管理（APIキー、マージ検証など）には一切触れず、スタイリングのみを DESIGN_SYSTEM に従うようにアップデートしました。
- **データ取得と状態**: データベースのロードが完了してから、`projects` を取得するフローを追加しています。初期シードデータが想定通り表示されることを確認しました。
- **一時ファイルの削除**: 作業中に使用した一時スクリプト等やデータベースファイルは削除し、リポジトリが汚れないように整理しました。

ご確認いただき、問題なければコミット・プッシュをお願いいたします。よろしくお願いいたします。
