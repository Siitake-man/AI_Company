# Jules向け指示書 — 2026-07-18（AIカンパニー）

## 対象ブランチ・コミット
- ブランチ: main
- 直前のコミット: 設定画面の2ペイン化およびTauri v2権限の修正（本日の作業終了時点のコミット）

## 依頼するタスク（夜間バッチ開発）

今回はUIの微修正にとどまらず、**コード品質の根本的な向上、セキュリティ・パフォーマンスの最適化、および本来予定していたタスクの実行**まで踏み込んで対応をお願いします。

### 1. コードの可読性向上とリファクタリング
- **対象**: `src/components/` 配下の主要コンポーネント（特に `SettingsScreen.tsx`, `MeetingScreen.tsx`, `HomeScreen.tsx` などコードが肥大化しているもの）。
- **作業内容**: 
  - 重複しているロジック（例えばAPIキーの保存処理やTauriコマンド呼び出し等）をカスタムフック（`src/hooks/`）やユーティリティ関数（`src/lib/`）に切り出してください。
  - コメントの整理、命名規則の統一、不要な`console.log`やコメントアウトされたデッドコードの削除を行い、徹底的にクリーンアップしてください。

### 2. セキュリティとパフォーマンスの向上
- **作業内容**:
  - Reactの不要な再レンダリングを防ぐため、適切な箇所への `useMemo`, `useCallback`, `React.memo` の導入を検討・実施してください。
  - Tauriからの非同期呼び出し（SQLiteなど）において、エラーハンドリングが不十分な箇所（例: `try-catch` がない、ユーザーへの適切なフィードバックがない）を堅牢にしてください。
  - アプリ全体のロード時間や体感速度向上のための最適化（画像や重いレンダリングの遅延ロードなど）があれば実施してください。

### 3. UIの洗練（余白と視認性の向上）
- **対象**: `src/components/SettingsScreen.tsx` を中心とする全般。
- **作業内容**:
  - 本日の昼の作業で2ペイン化・タブ統合を行いましたが、まだ窮屈です。「UIとして余白を意識したデザイン」にブラッシュアップしてください。
  - 各セクション間のマージン、フォーム周りの余白、文字の視認性（フォントサイズや行間）を「かわいい手帳風」「モダンなSaaS風」に洗練させてください。

### 4. グローバルSKILL（コンテキスト自動生成）の高度化
- **対象**: `C:\Users\bonob\.gemini\config\skills\generate-ai-company-context\SKILL.md`
  *(※ローカルのSKILLファイルは削除済みのため、必ず上記グローバルパスのファイルを編集すること)*
- **作業内容**: 
  - 現在のSKILLは「`docs/` や `README.md` を動的に探す」という指示に留まっています。Jules自身の高度な探索能力（Codebase-memory-mcpやgrep等）をフル活用できるよう、**「どのツールを使ってどう探索すべきか」の具体的なプロンプト指示**を追記・洗練させてください。
  - 出力フォーマット（コアプロフィール）について、AIがより「PM視点」「論理的・本質的な議論」を展開しやすくなるよう、行動指針や抽出条件を強化してください。

### 5. ROADMAP「Phase 2」へ向けた準備（本来予定していたタスク）
- **対象**: `docs/design/ROADMAP.md` および `docs/design/DESIGN_SPEC.md`
- **作業内容**:
  - 本日でPhase 1（基本UI・データベース実装・AI会議体験の基盤）が完了しました。
  - ROADMAPに定義されている「ノードJ（Phase 2検討開始）」に従い、次に着手予定である「RAGの導入」「LangChain.jsの本格導入」「より深いプロンプトマージロジック」に向けた**事前準備（ディレクトリの掘り下げ、設計書のアップデート、必要なダミーファイルの配置など）**を実施してください。

## 作業上の制約（必ず守ること）
- 3層構造（プロジェクト→部署→メンバー）と4層マージロジックは絶対に変更しない。
- `src-tauri/` 配下の Rust コードおよび `capabilities/default.json` の設定は触らない（本日の作業で安定したため）。
- ハードコード禁止（デザイントークン・プリセットは `index.css` の `--color-*` 変数等で管理）。

## 設計資料の参照先
- DESIGN_SPEC.md: `docs/design/DESIGN_SPEC.md`
- ROADMAP.md: `docs/design/ROADMAP.md`
- AI_RULES.md: `docs/design/AI_RULES.md`
- 本日の学習メモ: `docs/learning/学習メモ_20260718.md`

## 完了条件と確認方法
- 上記1〜5のすべてのタスクについて、コミットが分かりやすく分割されていること。
- グローバルの `generate-ai-company-context/SKILL.md` の指示内容が高度な探索手順にアップデートされていること。
- Viteでのビルド (`npm run build`) がエラーなく通ること。
