# Jules向け指示書 — 2026-07-18（AIカンパニー）

## 対象ブランチ・コミット
- ブランチ: main
- 直前のコミット: 設定画面の2ペイン化およびTauri v2権限の修正（本日の作業終了時点のコミット）

## 依頼するタスク
今回はアプリケーションのコードそのものではなく、AIエージェント（私たち）自身の機能を拡張するSKILL定義書の見直しと、アプリの軽微なUI調整をお願いします。

1. **コンテキスト生成SKILL（`SKILL.md`）のレビューと改善**
   - **対象ファイル**: `C:\Users\bonob\.gemini\config\skills\generate-ai-company-context\SKILL.md` （※環境によっては `~/.gemini/config/...`。見つからない場合はプロジェクト直下の残骸ではなく、グローバルフォルダの同ファイルを参照すること）
   - **作業内容**: 
     - 現在のSKILLは「`docs/` や `README.md` を動的に探す」という指示になっていますが、実際のJulesの高度な探索能力（Codebase-memory-mcpやgrep等）をフル活用できるように、**「どのツールを使ってどう探索すべきか」の具体的なプロンプト指示**を追記・洗練させてください。
     - 出力フォーマット（コアプロフィール）について、AIカンパニー内のエージェントがより「PM視点」「論理的・本質的な議論」を展開しやすくなるよう、行動指針やプロンプトの抽出条件（AIの推論を深く引き出すようなプロンプト）の指示を強化してください。
     - 変更内容をSKILL.mdに反映してください。

2. **設定画面のUI微調整（余白と視認性の向上）**
   - **対象ファイル**: `src/components/SettingsScreen.tsx`
   - **作業内容**:
     - 本日の昼の作業で2ペイン化・タブ統合・余白（padding/gap）の拡大を行いましたが、ユーザーから「UIとして余白を意識したUIにしてもらうことでもっと使いやすく・視認性が良くなると思う」とのフィードバックがありました。
     - CSS/Tailwindクラスを見直し、各セクション（APIキー管理、モデル一括適用など）間のマージンや、フォーム周りの余白、文字の視認性（フォントサイズや行間）をさらに洗練された「かわいい手帳風」「モダンなSaaS風」にブラッシュアップしてください。機能は一切変更しないでください。

## 作業上の制約（必ず守ること）
- 3層構造（プロジェクト→部署→メンバー）と4層マージロジックは絶対に変更しない。
- `src-tauri/` 配下の Rust コードおよび `capabilities/default.json` の設定は触らない（本日の作業で安定したため）。
- ハードコード禁止（デザイントークン・プリセットは `index.css` の `--color-*` 変数等で管理）。

## 設計資料の参照先
- DESIGN_SPEC.md: `docs/design/DESIGN_SPEC.md`
- AI_RULES.md: `docs/design/AI_RULES.md`
- 本日の学習メモ: `docs/learning/学習メモ_20260718.md`

## 完了条件と確認方法
- グローバルの `generate-ai-company-context/SKILL.md` の指示内容がより具体的で高度な探索手順にアップデートされていること。
- 設定画面 (`SettingsScreen.tsx`) のUIが、機能性を保ちつつも余白が美しく調整されていること。
- Viteでのビルド (`npm run build`) がエラーなく通ること。

## 注意・申し送り
- SKILLの更新を行う際は、ユーザーがグローバルに配置したファイルのパスを正確に取得し、上書きしてください。ローカル（プロジェクト内）に残っている古いSKILL.mdは無視してください。
- ユーザーは「隙間時間の5分で完結」を重要視しています。大掛かりなリファクタリングではなく、的確な改善に留めてください。
